# Multi-Agent Orchestration

A research preview feature allowing one coordinator agent to invoke and delegate tasks to specialist sub-agents.

## Details

Configured via callable_agents in the agent definition. A coordinator agent can invoke other agents as subtasks, enabling the multi-agent swarm pattern managed entirely within Anthropic's infrastructure. Currently in limited research preview requiring access request.
