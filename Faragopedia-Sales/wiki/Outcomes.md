# Outcomes

A research preview feature allowing developers to define success criteria so agents can self-evaluate and iterate until criteria are met.

## Details

Outcomes are defined as part of the agent or session configuration. The agent evaluates its own work against the defined criteria and iterates until satisfied. Useful for tasks where quality judgment is required rather than simple binary pass/fail checks. Currently in limited research preview requiring access request.
