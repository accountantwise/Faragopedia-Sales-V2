# CLAUDE.md

> **⚠️ This file is for Claude-specific configuration only.**
> All project context, conventions, and status live in [`AGENTS.md`](AGENTS.md).
> Read that file FIRST at the start of every session.

## Mandatory First Steps

1. Read [`AGENTS.md`](AGENTS.md) for full project context.
2. Read [`docs/status.md`](docs/status.md) for current project state.
3. Check [`docs/decisions/`](docs/decisions/) for architectural decisions.

## Claude-Specific Notes

- When completing work, update `docs/status.md` with what was done.
- Record any architectural decisions as ADRs in `docs/decisions/`.
- Do NOT store project-wide context only in Claude's memory — put it in
  `AGENTS.md` or `docs/` so Gemini (and any other agent) can access it too.
