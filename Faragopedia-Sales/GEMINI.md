# GEMINI.md

> **⚠️ This file is for Gemini-specific configuration only.**
> All project context, conventions, and status live in [`AGENTS.md`](AGENTS.md).
> Read that file FIRST at the start of every session.

## Mandatory First Steps

1. Read `AGENTS.md` for full project context.
2. Read `docs/status.md` for current project state.
3. Check `docs/decisions/` for architectural decisions.

## Gemini-Specific Notes

- When completing work, update `docs/status.md` with what was done.
- Record any architectural decisions as ADRs in `docs/decisions/`.
- Do NOT store project-wide context only in Gemini's memory — put it in
  `AGENTS.md` or `docs/` so Claude (and any other agent) can access it too.
